{
  "name": "drivevideosetup-legal-streamsaver",
  "version": "1.0.0",
  "private": true,
  "description": "Transparent StreamSaver download page for DriveVideoSetup.",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "vendor": "node scripts/vendor-streamsaver.mjs",
    "prepare": "npm run vendor",
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "check:source": "node scripts/check.mjs --source-only"
  },
  "dependencies": {
    "streamsaver": "2.0.6"
  }
}
